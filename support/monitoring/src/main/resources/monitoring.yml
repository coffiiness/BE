management:
  endpoints:
    web:
      exposure:
        include:
          - prometheus
          - health
          - metrics

  endpoint:
    prometheus:
      enabled: true

  metrics:
    tags:
      application: calfit-api