bootJar.enabled = false
jar.enabled = true

dependencies {
    implementation project(':core:core-enum')
    implementation project(':core:domain-recruitment')

    implementation 'org.springframework.boot:spring-boot-starter-web'
}
